# CodeDojo

CodeDojo is an open-source training ground for developers in the AI era.

This repository is in active MVP development. The current spine includes a CLI,
local sandbox, mock coach, session persistence, and a smoke test.

```sh
make smoke
```
